--// Flshty Hub GRATUIT + VERSION AVEC KEY //
--by flashtynooo
--and by my flshty_testers

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local mouse = player:GetMouse()
local UIS = game:GetService("UserInputService")
local Clipboard = setclipboard or toclipboard

-- Si un hub est déjà ouvert, le supprimer avant de relancer
local oldGui = game.CoreGui:FindFirstChild("FlshtyKeyHub")
if oldGui then
    oldGui:Destroy()
end

local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "FlshtyKeyHub"
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 300, 0, 250)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -125)
MainFrame.BackgroundColor3 = Color3.fromRGB(130, 0, 255)
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.Text = "FLSHTY HUB ACCESS"
Title.TextColor3 = Color3.fromRGB(57, 255, 20)
Title.BackgroundTransparency = 1
Title.TextScaled = true
Title.Font = Enum.Font.Code

local FreeButton = Instance.new("TextButton", MainFrame)
FreeButton.Size = UDim2.new(0.8, 0, 0, 40)
FreeButton.Position = UDim2.new(0.1, 0, 0.25, 0)
FreeButton.Text = "🚀 Start Flshty Hub for free"
FreeButton.BackgroundColor3 = Color3.fromRGB(200, 0, 200)
FreeButton.TextColor3 = Color3.fromRGB(0, 255, 255)
FreeButton.TextScaled = true
FreeButton.Font = Enum.Font.Arcade
local UICorner2 = Instance.new("UICorner", FreeButton)
UICorner2.CornerRadius = UDim.new(0, 8)

local CodeBox = Instance.new("TextBox", MainFrame)
CodeBox.Size = UDim2.new(0.8, 0, 0, 40)
CodeBox.Position = UDim2.new(0.1, 0, 0.45, 0)
CodeBox.PlaceholderText = "Enter code here!"
CodeBox.Text = ""
CodeBox.BackgroundColor3 = Color3.fromRGB(30, 0, 50)
CodeBox.TextColor3 = Color3.fromRGB(255, 255, 255)
CodeBox.TextScaled = true
CodeBox.Font = Enum.Font.Code
local UICorner3 = Instance.new("UICorner", CodeBox)
UICorner3.CornerRadius = UDim.new(0, 8)

local CopyButton = Instance.new("TextButton", MainFrame)
CopyButton.Size = UDim2.new(0.8, 0, 0, 40)
CopyButton.Position = UDim2.new(0.1, 0, 0.65, 0)
CopyButton.Text = "📋 Copy link Key System"
CopyButton.BackgroundColor3 = Color3.fromRGB(200, 0, 200)
CopyButton.TextColor3 = Color3.fromRGB(0, 255, 255)
CopyButton.TextScaled = true
CopyButton.Font = Enum.Font.Arcade
local UICorner4 = Instance.new("UICorner", CopyButton)
UICorner4.CornerRadius = UDim.new(0, 8)

local function launchFreeHub()
    -- Destroy le hub existant immédiatement si il existe encore
    local existing = game.CoreGui:FindFirstChild("FlshtyKeyHub")
    if existing then
        existing:Destroy()
    end

    -- Lancement du Free Flshty Hub complet
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Every-hub-every-games/Intro"))()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Every-hub-every-games/Copy"))()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Every-hub-every-games/Script%20total%202"))()
end

local function launchKeyHub()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Flshty-hub-discord-user/Hub%20flshty%20discord%20user"))()
end

FreeButton.MouseButton1Click:Connect(function()
    launchFreeHub()
    MainFrame:Destroy()
end)

CopyButton.MouseButton1Click:Connect(function()
    if Clipboard then
        Clipboard("https://link-hub.net/1366319/7R789PjMo6Sf")
    end
end)

CodeBox.FocusLost:Connect(function(enter)
    if enter then
        if CodeBox.Text == "%zX7ؚv_pTqyLؚ9kW3uF#tؚ2JrVbQx_s8MؚZnE4dؚLjA6cP" then
            launchKeyHub()
            MainFrame:Destroy()
        else
            CodeBox.Text = "❌ Code invalide"
            wait(1)
            CodeBox.Text = ""
        end
    end
end)
