-- Services
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

-- Création du ScreenGui
local gui = Instance.new("ScreenGui", game.Players.LocalPlayer:WaitForChild("PlayerGui"))
gui.Name = "FLSHTY_HACKER_HUB"
gui.ResetOnSpawn = false

-- Frame principale du hub
local main = Instance.new("Frame", gui)
main.Name = "MainFrame"
main.Size = UDim2.new(0, 350, 0, 250)
main.Position = UDim2.new(0.5, -175, 0.5, -125)
main.AnchorPoint = Vector2.new(0.5, 0.5)
main.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
main.BorderSizePixel = 2
main.BorderColor3 = Color3.fromRGB(50, 50, 50)
main.Active = true
main.Draggable = true

-- Frame de fond Matrix (en bas, ZIndex 0)
local matrixFrame = Instance.new("Frame", main)
matrixFrame.Name = "MatrixFrame"
matrixFrame.Size = UDim2.new(1, 0, 1, 0)
matrixFrame.Position = UDim2.new(0, 0, 0, 0)
matrixFrame.BackgroundTransparency = 1
matrixFrame.ZIndex = 0

-- TopBar contenant les boutons
local topBar = Instance.new("Frame", main)
topBar.Name = "TopBar"
topBar.Size = UDim2.new(1, 0, 0, 30)
topBar.Position = UDim2.new(0, 0, 0, 0)
topBar.BackgroundTransparency = 1
topBar.ZIndex = 2

local minimize = Instance.new("TextButton", topBar)
minimize.Name = "Minimize"
minimize.Size = UDim2.new(0, 30, 1, 0)
minimize.Position = UDim2.new(1, -60, 0, 0)
minimize.Text = "-"
minimize.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
minimize.TextColor3 = Color3.new(0, 0, 0)
minimize.Font = Enum.Font.Code
minimize.TextSize = 20
minimize.ZIndex = 3

local close = Instance.new("TextButton", topBar)
close.Name = "Close"
close.Size = UDim2.new(0, 30, 1, 0)
close.Position = UDim2.new(1, -30, 0, 0)
close.Text = "X"
close.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
close.TextColor3 = Color3.new(0, 0, 0)
close.Font = Enum.Font.Code
close.TextSize = 20
close.ZIndex = 3

-- Frame titre
local titleFrame = Instance.new("Frame", main)
titleFrame.Name = "TitleFrame"
titleFrame.Size = UDim2.new(1, 0, 0, 40)
titleFrame.Position = UDim2.new(0, 0, 0, 30)
titleFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
titleFrame.BorderSizePixel = 2
titleFrame.BorderColor3 = Color3.fromRGB(50, 50, 50)
titleFrame.ZIndex = 2

local titleLabel = Instance.new("TextLabel", titleFrame)
titleLabel.Name = "TitleLabel"
titleLabel.Size = UDim2.new(1, 0, 1, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "🕹️ FLSHTY-GAMES 🎮"
titleLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
titleLabel.Font = Enum.Font.Code
titleLabel.TextSize = 22
titleLabel.ZIndex = 3

-- Frame des boutons (scrollable)
local scrollableFrame = Instance.new("ScrollingFrame", main)
scrollableFrame.Name = "ScrollableFrame"
scrollableFrame.Position = UDim2.new(0, 0, 0, 75)
scrollableFrame.Size = UDim2.new(1, 0, 1, -75)
scrollableFrame.BackgroundTransparency = 1
scrollableFrame.CanvasSize = UDim2.new(0, 10, 0, 300) -- Ajuster selon la taille des éléments
scrollableFrame.ScrollBarThickness = 8
scrollableFrame.ZIndex = 2

local uiListLayout = Instance.new("UIListLayout", scrollableFrame)
uiListLayout.Padding = UDim.new(0, 10)
uiListLayout.FillDirection = Enum.FillDirection.Vertical
uiListLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Bouton Brookhaven
local brookhavenBtn = Instance.new("TextButton", scrollableFrame)
brookhavenBtn.Name = "Brookhaven"
brookhavenBtn.Size = UDim2.new(0.8, 0, 0, 40)
brookhavenBtn.Text = "Brookhaven"
brookhavenBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
brookhavenBtn.TextColor3 = Color3.new(0, 0, 0)
brookhavenBtn.Font = Enum.Font.Code
brookhavenBtn.TextSize = 20
brookhavenBtn.ZIndex = 3

brookhavenBtn.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Flashtynooo-hub/Brookhaven"))()
end)

-- Bouton CHICKEN JOCKEY!
local chickenJockeyBtn = Instance.new("TextButton", scrollableFrame)
chickenJockeyBtn.Name = "ChickenJockey"
chickenJockeyBtn.Size = UDim2.new(0.8, 0, 0, 40)
chickenJockeyBtn.Text = "CHICKEN JOCKEY!"
chickenJockeyBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
chickenJockeyBtn.TextColor3 = Color3.new(0, 0, 0)
chickenJockeyBtn.Font = Enum.Font.Code
chickenJockeyBtn.TextSize = 20
chickenJockeyBtn.ZIndex = 3

chickenJockeyBtn.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Flashtynooo-hub/Line%20to%20CHICKEN%20JOCKEY!"))()
end)

-- Bouton SLAP BATTLE
local slpBtn = Instance.new("TextButton", scrollableFrame)
slpBtn.Name = "SlapBattle"
slpBtn.Size = UDim2.new(0.8, 0, 0, 40)
slpBtn.Text = "Slap Battle"
slpBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
slpBtn.TextColor3 = Color3.new(0, 0, 0)
slpBtn.Font = Enum.Font.Code
slpBtn.TextSize = 20
slpBtn.ZIndex = 3

slpBtn.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Flashtynooo/Roblox/refs/heads/Slap-battle/Hub%20slap%20battle"))()
end)

-- Bouton Close
close.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

-- Minimize / Déminimiser
local minimizedState = false
local fullSize = UDim2.new(0, 350, 0, 250)
local minimizedSize = UDim2.new(0, 350, 0, 30)

minimize.MouseButton1Click:Connect(function()
    minimizedState = not minimizedState
    minimize.Text = minimizedState and "+" or "-"

    local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    local targetSize = minimizedState and minimizedSize or fullSize
    TweenService:Create(main, tweenInfo, {Size = targetSize}):Play()

    task.delay(0.1, function()
        titleFrame.Visible = not minimizedState
        scrollableFrame.Visible = not minimizedState
    end)
end)

-- (Optionnel) Tu peux ajouter ici la suite de ton effet Matrix si tu veux qu'on la complète.
